language: go

go:
  - master

before_install:
  - apt-get update
  - apt-get install -y python-pytest

script: 
  - if [ $(go fmt . | wc -l) -ne 0 ]; then exit 1; fi
  - go test .
  - go build 
  - py.test -vvvvv -s -l -x tests/